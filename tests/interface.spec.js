qwdsdasD2F2EDFWDC2D DDQDF2D console.warn(QDWCF2D);    W1D   1D          SDQ SD        // DEBUG: WD  qwdsdasD2F2EDFWDC2DWDXZX  case expression:
              ````      switch (switch (CC) {
                case switch (SC) {
                  case case switch (SCS) {
                    case CC:

                      break;
                    default:
                      case switch (switch (switch (switch (switch (switch (switch (SCSCSZ) {
                        case case case case expression:

                          break;:

                          break;:

                          break;:

                          break;
                        default:

                      }) {
                        case expression:

                          break;
                        default:

                      }) {
                        case expression:

                          break;
                        default:

                      }) {
                        case expression:

                          break;
                        default:

                      }) {
                        case expression:

                          break;
                        default:

                      }) {
                        case expression:

                          break;
                        default:

                      }) {
                        case expression:

                          break;
                        default:

                      }:

                        break;
                  }:

                    break;:

                    break;
                  default:

                }:

                  break;
                default:

              }) {
                case expression:

                  break;
                default:

              }
  break;
